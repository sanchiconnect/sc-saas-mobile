// Shown when an unapproved user tries to create a post, comment, react, or
// share. Posting privileges are unlocked once an admin approves the account
// (`DashboardSummary.isApproved`).
export const APPROVAL_REQUIRED_MESSAGE =
  'Your account needs admin approval before you can post.';
