API_BASE_URL=https://api-dev.tenants.sanchiconnect.com/
TENANT_SLUG=thub
APP_ENV=staging
# NOTE: verify_tenant on this host currently returns apiUrl=https://api.thub.sncnt.in/
# which does not resolve in public DNS. Update once the new infra is live.

